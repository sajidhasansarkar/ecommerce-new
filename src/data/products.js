// প্রোডাক্ট ডেটা MongoDB থেকে আসে — এখানে কিছু নেই।
// ক্যাটাগরির তালিকা CategoryContext থেকে আসে (API: GET /api/categories)।

// পুরনো কোডের সাথে ব্যাকওয়ার্ড কম্প্যাটিবিলিটির জন্য
export const products = []
export function getProductById() { return null }
